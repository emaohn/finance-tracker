import Grid from '@mui/material/Grid';
import Typography from '@mui/material/Typography';

export default function NetWorth () {
    return (
        <Grid>
            <Typography variant="h3" gutterBottom>Net Worth</Typography>
        </Grid>
    )
}